spring:
  application:
    name: ticket-system
  profiles:
    active: dev

server:
  port: 8080

app:
  cors:
    # Comma-separated; include both if you use localhost or 127.0.0.1 in the browser
    allowed-origins: ${FRONTEND_ORIGINS:http://localhost:3000,http://127.0.0.1:3000}
